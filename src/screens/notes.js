

// where do you need redux? global state. what for?
//

// class Game extends Component {
// instantiate constctuor
// state = {
//   truckAssets: [],
//   inventoryAssets: [],
// }
// asset handler will use setState to update state.
// call via setState({ asset, asset } => function)


// render <Inventory items={this.props.inventoryAssets}>
// render <Truck> same way ^^^ and also give it onAssetClick prop ({this.handleAssetClick})
// }

// class Inventory extends Component {
//   // props: asserts
//   // props: onAssetClick
//   render() {
//     this.props.assets.map(asset) => {
//       // do something.
//       <Asset onClick={this.props.onAssetClick} />
//     }
//   }
// }
